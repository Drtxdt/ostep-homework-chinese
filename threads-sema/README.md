# 概述

此目录中包含了一些代码框架片段，你可以填充这些框架来解决各种同步问题。

更多乐趣请参见 Allen Downey 的书籍 "A Little Book of Semaphores"。该书也是免费的！

要编译其中任意一个文件（例如，名为 `foo.c`的文件）：

```
prompt> gcc -o foo foo.c -Wall -pthread
```

然后，运行它：

```
prompt> ./foo
```

（可能带有一些可选参数）
