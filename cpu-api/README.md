
# 概述

本章现提供两个相关模拟器。第一个`fork.py` 是一个简单工具，用于展示进程在创建和销毁时进程树的形态。更多详情请参阅[此处](README-fork.md)。

第二个程序 generator.py 会生成真正的 C 程序，这些程序使用 `fork()`、`wait()` 和 `exit()` 函数，展示 fork 在运行程序中的工作原理。更多详情请参阅此处。











